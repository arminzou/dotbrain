# agents/

Per-project agent operating conventions.

Project-specific deviations and references the agent reads while working: the
issue-tracker conventions (`issue-tracker.md`), labels, and similar. Skill
*selection* is not configured here; per-project skills live in `project.yaml`
under `skills:`.

See `DOTBRAIN.md` for the read order and operating rules.
